.page-header
  = link_to new_pet_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Pet
  %h1 Listing pets

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Pet kind
        %th Breed
        %th Dog lg
        %th Dog md
        %th Dog sm
        %th Cat
        %th Other
        %th User
        %th Sitter
        %th{colspan: 3}

    %tbody
      - @pets.each do |pet|
        %tr
          %td= pet.pet_kind
          %td= pet.breed
          %td= pet.dog_lg
          %td= pet.dog_md
          %td= pet.dog_sm
          %td= pet.cat
          %td= pet.other
          %td= pet.user_id
          %td= pet.sitter_id
          %td
            = link_to pet do
              %span.glyphicon.glyphicon-ok
          %td
            = link_to edit_pet_path(pet) do
              %span.glyphicon.glyphicon-pencil
          %td
            = link_to pet, data: { confirm: 'Are you sure?' }, method: :delete do
              %span.glyphicon.glyphicon-remove
