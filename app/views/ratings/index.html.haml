.page-header
  = link_to new_rating_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Rating
  %h1 Listing ratings

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Star
        %th User
        %th Sitter
        %th{colspan: 3}

    %tbody
      - @ratings.each do |rating|
        %tr
          %td= rating.star
          %td= rating.user_id
          %td= rating.sitter_id
          %td
            = link_to rating do
              %span.glyphicon.glyphicon-ok
          %td
            = link_to edit_rating_path(rating) do
              %span.glyphicon.glyphicon-pencil
          %td
            = link_to rating, data: { confirm: 'Are you sure?' }, method: :delete do
              %span.glyphicon.glyphicon-remove
